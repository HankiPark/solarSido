<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="solar.cmm.rscinfer.service.impl.RscinferMapper">
	<select id="rscinferList">
		SELECT * FROM RSC_INFER_CD
    </select>

    <insert id="insert">
		INSERT INTO RSC_INFER_CD(RTNGD_RESN_CD, RSC_INFER_NM, RSC_INFER_DESCT) 
		VALUES (#{RTNGD_RESN_CD }, #{RSC_INFER_NM}, #{RSC_INFER_DESCT })
	</insert>

    <update id="update">
		UPDATE	RTNGD_RESN_CD
		SET RSC_INFER_NM = #{RSC_INFER_NM }
			, RSC_INFER_DESCT = #{RSC_INFER_DESCT}
		WHERE RTNGD_RESN_CD = #{RTNGD_RESN_CD }	
	</update>

    <delete id="delete">
      DELETE FROM RSC_INFER_CD
      WHERE RTNGD_RESN_CD = #{RTNGD_RESN_CD}
   </delete>
</mapper>