<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="solar.cmm.prdt.service.impl.CmmPrdtMapper">
	<select id="prdtList" resultType="solar.cmm.prdt.dao.PrdtInferVO">
		SELECT * FROM PRDT_INFER_CD 
    </select>

    <insert id="insert">
		INSERT INTO PRDT_INFER_CD(PRDT_INFER_CD, PRCS_INFER_CD, PRDT_INFER_NM, PRDT_INFER_DESCT) 
				VALUES (#{PRDT_INFER_CD }, #{PRCS_INFER_CD}, #{PRDT_INFER_NM }, #{PRDT_INFER_DESCT })
	</insert>

    <update id="update">
		UPDATE	PRDT_INFER_CD
		SET PRCS_INFER_CD = #{PRCS_INFER_CD }
			, PRDT_INFER_NM = #{PRDT_INFER_NM}
            , PRDT_INFER_DESCT = #{PRDT_INFER_DESCT}
		WHERE PRDT_INFER_CD = #{PRDT_INFER_CD }
	</update>

     <delete id="delete">
      DELETE FROM PRDT_INFER_CD
      WHERE PRDT_INFER_CD = #{PRDT_INFER_CD}
   </delete>
</mapper>